# Managed by `make secrets HOST=castitas`
# Each host gets its own encrypted secrets file keyed to its SSH host key.
# Run `make secrets HOST=castitas` to (re)generate this file.
#
# Unencrypted structure:
# user:
#     hashedPassword: <mkpasswd output>
